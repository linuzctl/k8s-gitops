---
apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: keycloak-postgres-backup
  namespace: keycloak
spec:
  cluster:
    name: keycloak-postgres-17
  schedule: "0 0 10 * * *"  # At 10:00 every day
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io
