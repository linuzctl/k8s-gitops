---
apiVersion: v1
kind: Service
metadata:
  name: minio
  namespace: gitlab
spec:
  ports:
    - name: http-minio
      port: 80
      protocol: TCP
      targetPort: 9000
  selector:
    aistor.min.io/objectStore: gitlab-aistor
  type: ClusterIP
