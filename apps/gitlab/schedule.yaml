---
apiVersion: velero.io/v1
kind: Schedule
metadata:
  name: gitlab-backup
  namespace: velero
spec:
  schedule: '@every 24h'
  template:
    csiSnapshotTimeout: 0s
    includedNamespaces:
      - gitlab
    snapshotMoveData: true
    storageLocation: backup-aistor
    volumeSnapshotLocations:
      - ceph-block
  useOwnerReferencesInBackup: false
