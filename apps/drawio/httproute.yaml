---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: drawio
  namespace: drawio
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: ListenerSet
      name: drawio
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: drawio
          port: 8080
