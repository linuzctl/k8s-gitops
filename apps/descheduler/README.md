# descheduler

The Descheduler is an Kubernetes component that removes (evicts) Pods from nodes when the cluster becomes unbalanced or when certain rules are violated.

source: [GitHub](https://github.com/kubernetes-sigs/descheduler)
