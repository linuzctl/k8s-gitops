---
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: outline-postgres
  namespace: outline
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: outline-postgres
  podMetricsEndpoints:
    - port: metrics
