---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: descheduler
  namespace: descheduler
spec:
  interval: 20m
  driftDetection:
    mode: enabled
  chart:
    spec:
      chart: descheduler
      version: v0.34.0
      sourceRef:
        kind: HelmRepository
        name: descheduler
        namespace: flux-system
      interval: 20m
  values:
    serviceMonitor:
      enabled: true
