---
apiVersion: gateway.networking.k8s.io/v1
kind: ListenerSet
metadata:
  name: drawio
  namespace: drawio
spec:
  parentRef:
    namespace: envoy-gateway-system
    name: envoy-gateway
    kind: Gateway
    group: gateway.networking.k8s.io
  listeners:
    - name: drawio
      hostname: drawio.${DOMAIN}
      protocol: HTTPS
      port: 443
      tls:
        mode: Terminate
        certificateRefs:
          - kind: Secret
            name: drawio-${DOMAIN/./-}-tls
