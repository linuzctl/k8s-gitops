---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cloudnative-pg
  namespace: cnpg-system
spec:
  interval: 20m
  driftDetection:
    mode: enabled
  chartRef:
    kind: OCIRepository
    name: cloudnative-pg
    namespace: cnpg-system
  values:
    crds:
      create: false
  install:
    crds: Skip
  upgrade:
    crds: Skip
