# coredns

CoreDNS is the default DNS server in Kubernetes, providing service discovery by resolving DNS names to IP addresses within the cluster.

source: [GitHub](https://github.com/coredns/coredns)
